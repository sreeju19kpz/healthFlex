import { Measurements } from "../../constants/Measurements";
import { StyleSheet } from "react-native";

export const height = StyleSheet.create({
  heightScreen: {
    height: Measurements.SCREEN_WIDTH,
  },
  heightWindow: {
    height: Measurements.SCREEN_WIDTH,
  },
});
