import { StyleSheet } from "react-native";

export const componentStyles = StyleSheet.create({
  label: { fontSize: 14, lineHeight: 16 },
  absolute_fill: { position: "absolute", inset: 0 },
  title: { fontSize: 12, lineHeight: 14.4 },
});
